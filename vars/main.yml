---
# vars file for timescaledb

timescaledb__apt_repo: "deb https://packagecloud.io/timescale/timescaledb/{{ ansible_distribution | lower }}/ {{ ansible_distribution_release }} main"
timescaledb__gpg_url: https://packagecloud.io/timescale/timescaledb/gpgkey
